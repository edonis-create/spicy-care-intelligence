/** TopBar headline + sidebar project name — keep in sync (one step above `text-sm`, below `text-base`). */
export const CHROME_HEADLINE =
  "text-[15px] font-medium leading-snug tracking-tight text-[var(--text-primary)]";

/** TopBar “Data generated …” line */
export const CHROME_META = "text-xs leading-snug text-[var(--text-tertiary)]";
